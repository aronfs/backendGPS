meta {
  name: Crear Categoria
  type: http
  seq: 1
}

post {
  url: {{url_backend}}api/v1/items/getItems
  body: json
  auth: bearer
}

auth:bearer {
  token: {{Authorization}}
}

body:json {
  {
    "type_item": "",
    "icon_item": ""
  }
}

settings {
  encodeUrl: true
}
