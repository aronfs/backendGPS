meta {
  name: Crear Categoria
  type: http
  seq: 1
}

post {
  url: {{url_backend}}api/v1/items/createItems
  body: json
  auth: bearer
}

auth:bearer {
  token: {{Authorization}}
}

body:json {
  {
    "type_item": "categoria1",
    "icon_item": "icon.png"
  }
}

settings {
  encodeUrl: true
}
