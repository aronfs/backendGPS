meta {
  name: Activar FCM
  type: http
  seq: 5
}

patch {
  url: {{url_backend}}/api/v1/accounts/activateFCMAccount?isactivate=false
  body: none
  auth: bearer
}

params:query {
  isactivate: false
}

auth:bearer {
  token: {{Authorization}}
}

settings {
  encodeUrl: true
}
