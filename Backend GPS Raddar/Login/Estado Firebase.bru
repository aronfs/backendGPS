meta {
  name: Estado Firebase
  type: http
  seq: 6
}

get {
  url: {{url_backend}}/api/v1/accounts/statusFCM
  body: none
  auth: bearer
}

auth:bearer {
  token: {{Authorization}}
}

settings {
  encodeUrl: true
}
