meta {
  name: Crear Menu
  type: http
  seq: 1
}

post {
  url: {{url_backend}}api/v1/menu/createMenu
  body: json
  auth: bearer
}

auth:bearer {
  token: {{Authorization}}
}

body:json {
  {
    "idMenu": 2,
    "nombre": "Mis Vehiculos",
    "icono": "time_to_leave",
    "url": "/pages/productos",
    "rol": "Colaborador"
  }
}

settings {
  encodeUrl: true
}
